# Apache — an open web landing page

A warm, editorial landing page celebrating Apache's open-source ecosystem and community.

## Run locally

Open `index.html` in a browser, or serve the directory with any static server:

```bash
python3 -m http.server 8000
```

The page is built with vanilla HTML, CSS, and JavaScript, so it has no build step or dependencies.
